# MCP 服务依赖。
#
# 服务只做「配置解析 + 进程管理 + 读结果」,不需要 torch —— 起训练时会用
# OPENCSLR_PYTHON 指向训练环境的解释器(默认就是本进程的解释器)。
# 因此这套依赖可以装在任意一个干净的 python 里,与训练环境解耦。
#
# PyYAML 已在仓库根 requirements.txt 中,这里显式列出以免单独装时漏掉。
mcp>=2.0
PyYAML
